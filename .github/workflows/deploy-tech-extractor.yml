# @format
# Deploy Tech Extractor Worker (Development)
# Builds the tech-extractor container, pushes to ECR, writes image URI to SSM.
# Prerequisite: ECR repo provisioned via shared-infra CDK stack
#   (/shared/ecr-tech-extractor/development/repository-uri).

name: "Deploy Tech Extractor (Dev)"

on:
  push:
    branches: [develop]
    paths:
      - "applications/tech-extractor/**"
      - "applications/ingestion/**"
      - "applications/shared/**"
      - ".github/workflows/deploy-tech-extractor.yml"
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

concurrency:
  group: deploy-tech-extractor-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    uses: ./.github/workflows/_build-push-image.yml
    with:
      app-name: tech-extractor
      dockerfile: applications/tech-extractor/Dockerfile
      ecr-ssm-path: /shared/ecr-tech-extractor/development/repository-uri
      image-ssm-path: /k8s/development/job-images/tech-extractor
    secrets:
      AWS_OIDC_ROLE: ${{ secrets.AWS_OIDC_ROLE }}

  deploy-marker:
    name: Loki deploy marker
    needs: [deploy]
    if: always()
    runs-on: ubuntu-latest
    timeout-minutes: 2
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Compute aggregate status
        id: agg
        env:
          STATUSES: ${{ toJSON(needs) }}
        run: |
          if echo "$STATUSES" | jq -e 'to_entries | any(.value.result == "failure" or .value.result == "cancelled")' > /dev/null; then
            echo "status=failed" >> "$GITHUB_OUTPUT"
          else
            echo "status=succeeded" >> "$GITHUB_OUTPUT"
          fi
      - uses: ./.github/actions/loki-deploy-marker
        env:
          LOKI_PUSH_URL:        ${{ secrets.LOKI_PUSH_URL }}
          LOKI_PUSH_BASIC_AUTH: ${{ secrets.LOKI_PUSH_BASIC_AUTH }}
          PUSHGATEWAY_URL: ${{ secrets.PUSHGATEWAY_URL }}
          PUSHGATEWAY_BASIC_AUTH: ${{ secrets.PUSHGATEWAY_BASIC_AUTH }}
        with:
          service:     tech-extractor
          environment: development
          status:      ${{ steps.agg.outputs.status }}
          git-sha:     ${{ github.sha }}
          notes:       "tech-extractor image push"
