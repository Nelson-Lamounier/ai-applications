# Offline RAG scorers — read the JSONL emitted by run-rag-eval.ts and score it.
# Pin loosely; bump after a validating run. boto3 is only needed for the Bedrock
# BYOI converter/job-create path.
deepeval>=2.0,<4.0
ragas>=0.2,<0.4
datasets>=2.0
boto3>=1.34
# Only for persist_eval.py (RAG_EVAL_PERSIST=1 → rag_eval_runs).
psycopg[binary]>=3.1
